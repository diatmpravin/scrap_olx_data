<%= form_for(@wagonr) do |f| %>
  <% if @wagonr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wagonr.errors.count, "error") %> prohibited this wagonr from being saved:</h2>

      <ul>
      <% @wagonr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :base_link %><br>
    <%= f.text_field :base_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
